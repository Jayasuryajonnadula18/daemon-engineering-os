import chalk from 'chalk';

export async function runGraph(): Promise<void> {
  console.log(chalk.cyan.bold('Rendering dependency graph...'));
  console.log(chalk.yellow('This feature is coming soon.'));
}
